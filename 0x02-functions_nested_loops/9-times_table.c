#include "main.h"

/**
 * times_table - back to basic 1
 *
 * Return: none
 */
void times_table(void)
{
	int r, c, n;
	for (r = 0; r < 10; r++)
	{
		for (c = 0; c < 10; c++)
		{
			for (n = 0; n <= 9; n++)
			{
				int d = (c * n)
				






				if (n < 9)
				{
					_putchar(44);
					_putchar(32);
				}
				else
				{
					;
				}
			}
			_putchar(10);
		}
	}
}
