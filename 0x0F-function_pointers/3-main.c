#include "3-calc.c"

/**
 * main - calls the func
 * @argc: no of args
 * @argv: string pointer to arguments
 *
 * Return: the result of an operation
 */
int main(int argc, char *argv[])
{
	if (argc != 4)
	{
		printf("Error\n");
		exit (98);
	}

	








}
